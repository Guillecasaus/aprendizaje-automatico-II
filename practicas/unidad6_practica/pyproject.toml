[project]
name = "gmail-mcp-server"
version = "0.1.0"
description = "Servidor MCP para gestionar Gmail con Claude Desktop"
requires-python = ">=3.10"
dependencies = [
    "fastmcp",
    "google-auth-oauthlib",
    "google-api-python-client",
]
